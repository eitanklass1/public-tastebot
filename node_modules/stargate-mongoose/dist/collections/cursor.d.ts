import { Collection } from './collection';
import { FindOptions } from './options';
export declare class FindCursor {
    collection: Collection;
    filter: Record<string, any>;
    options: FindOptions;
    documents: Record<string, any>[];
    status: string;
    nextPageState?: string;
    limit: number;
    page: Record<string, any>[];
    pageIndex: number;
    exhausted: boolean;
    /**
   *
   * @param collection
   * @param filter
   * @param options
   */
    constructor(collection: Collection, filter: Record<string, any>, options?: FindOptions);
    /**
   *
   * @returns void
   */
    private getAll;
    /**
   *
   * @returns Record<string, any>[]
   */
    toArray(): Promise<any[]>;
    /**
   * @returns Promise
   */
    next(): Promise<any>;
    _getMore(): Promise<void>;
    /**
   *
   * @param iterator
   */
    forEach(iterator: any): Promise<any>;
    /**
   *
   * @param options
   * @returns Promise<number>
   */
    count(_options?: any): Promise<any>;
    /**
   *
   * @param options
   */
    stream(_options?: any): void;
}
