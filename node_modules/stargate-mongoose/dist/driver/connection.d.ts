import { Client } from '../collections/client';
import { default as MongooseConnection } from 'mongoose/lib/connection';
export declare class Connection extends MongooseConnection {
    debugType: string;
    initialConnection: Promise<Connection> | null;
    constructor(base: any);
    _waitForClient(): Promise<void>;
    collection(name: string, options: any): any;
    createCollection(name: string, options?: Record<string, any>): Promise<any>;
    dropCollection(name: string): Promise<any>;
    dropDatabase(): Promise<any>;
    openUri(uri: string, options: any): Promise<this>;
    createClient(uri: string, options: any): Promise<this>;
    setClient(client: Client): void;
    asPromise(): Promise<Connection> | null;
    /**
     *
     * @returns Client
     */
    doClose(_force?: boolean): this;
}
